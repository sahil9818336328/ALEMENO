import styled from 'styled-components'

export const StyledWrapper = styled.div`
  .logo {
    .logo-name {
      text-transform: uppercase;
      font-size: 1.5rem;
      font-weight: 600;
    }
  }
`
